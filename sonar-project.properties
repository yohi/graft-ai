# SonarCloud Project Configuration
sonar.projectKey=yohi_graft-ai
sonar.organization=yohi
sonar.projectName=graft-ai

# Path to source and test directories
sonar.sources=workers/src,deploy
sonar.tests=workers/tests,tests

# Test exclusions from duplication (CPD) checks
sonar.cpd.exclusions=workers/tests/**,tests/**

# General exclusions
sonar.exclusions=**/node_modules/**,**/dist/**,**/.wrangler/**,**/.terraform/**
