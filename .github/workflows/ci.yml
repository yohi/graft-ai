name: CI

on:
  pull_request:
    branches: [master]
  push:
    branches-ignore: [master]

jobs:
  checks:
    name: Test / Typecheck / Format / Terraform Validate
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Setup Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: workers/package-lock.json

      - name: Install Worker dependencies
        working-directory: workers
        run: npm ci --ignore-scripts

      - name: Type check Workers
        working-directory: workers
        run: npm run typecheck:ci

      - name: Test dedicated OTel Worker
        working-directory: workers
        run: |
          npm run test:otel
          npm run test:otel:r2
          npm run test:otel:kv-r2-drain

      - name: Type check dedicated OTel Worker
        working-directory: workers
        run: npm run typecheck:otel

      - name: Validate dedicated OTel Worker configuration
        run: make otel-worker-validate

      - name: Run repository tests
        run: make test

      - name: Check formatting
        working-directory: workers
        run: npm run fmt:check

      - name: Install actionlint
        env:
          ACTIONLINT_VERSION: 1.7.7
          ACTIONLINT_SHA256: 023070a287cd8cccd71515fedc843f1985bf96c436b7effaecce67290e7e0757
        run: |
          archive="actionlint_${ACTIONLINT_VERSION}_linux_amd64.tar.gz"
          archive_path="$RUNNER_TEMP/$archive"
          curl --fail --location --silent --show-error \
            --proto '=https' --tlsv1.2 \
            --output "$archive_path" \
            "https://github.com/rhysd/actionlint/releases/download/v${ACTIONLINT_VERSION}/${archive}"
          printf '%s  %s\n' "$ACTIONLINT_SHA256" "$archive_path" | sha256sum --check
          tar --extract --gzip --file "$archive_path" --directory "$RUNNER_TEMP"
          echo "$RUNNER_TEMP" >> "$GITHUB_PATH"

      - name: Check workflows and shell scripts
        run: |
          actionlint
          shellcheck scripts/*.sh terraform/*.sh tests/*.sh
          bash -n scripts/*.sh terraform/*.sh tests/*.sh

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@b9cd54a3c349d3f38e8881555d616ced269862dd # v3
        with:
          terraform_version: "1.10.0"
          terraform_wrapper: false

      - name: Terraform fmt check
        run: terraform fmt -check -recursive

      - name: Terraform validate (cloudflare)
        run: |
          terraform -chdir=terraform init -backend=false
          terraform -chdir=terraform validate

      - name: Terraform validate (grafana)
        run: |
          terraform -chdir=terraform/grafana init -backend=false
          terraform -chdir=terraform/grafana validate

  plan-cloudflare:
    name: Terraform Plan (cloudflare)
    runs-on: ubuntu-latest
    needs: [checks]
    if: github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository && github.actor != 'dependabot[bot]'
    env:
      TF_TOKEN_app_terraform_io: ${{ secrets.TF_API_TOKEN }}
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_READONLY_API_TOKEN }}
      CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      TF_VAR_cloudflare_account_id: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
      TF_VAR_cloudflare_api_token: ${{ secrets.CLOUDFLARE_READONLY_API_TOKEN }}
      TF_VAR_grafana_cloud_loki_url: ${{ secrets.GRAFANA_CLOUD_LOKI_READONLY_URL }}
      TF_VAR_grafana_cloud_loki_username: ${{ secrets.GRAFANA_CLOUD_LOKI_READONLY_USERNAME }}
      TF_VAR_grafana_cloud_access_policy_token: ${{ secrets.GRAFANA_CLOUD_LOKI_READONLY_TOKEN }}
      TF_VAR_origin_secret: ${{ secrets.ORIGIN_SECRET }}
      TF_VAR_rsa_private_key_pem: ${{ secrets.RSA_PRIVATE_KEY_PEM }}
      TF_VAR_workers_subdomain: ${{ vars.WORKERS_SUBDOMAIN }}
      TF_VAR_logpush_dataset: ${{ vars.LOGPUSH_DATASET || 'ai_gateway_events' }}
      TF_VAR_worker_script_name: ${{ vars.WORKER_SCRIPT_NAME || 'graft-ai-aig-logpush' }}
      TF_VAR_logpush_job_name: ${{ vars.LOGPUSH_JOB_NAME || 'graft-ai-aig-logpush' }}
    steps:
      - name: Checkout
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@b9cd54a3c349d3f38e8881555d616ced269862dd # v3
        with:
          terraform_version: "1.10.0"
          terraform_wrapper: false

      - name: Check Terraform plan inputs
        id: terraform_plan_inputs
        run: |
          missing_inputs=""
          for input_name in \
            TF_TOKEN_app_terraform_io \
            TF_VAR_cloudflare_account_id \
            TF_VAR_cloudflare_api_token \
            TF_VAR_grafana_cloud_loki_url \
            TF_VAR_grafana_cloud_loki_username \
            TF_VAR_grafana_cloud_access_policy_token \
            TF_VAR_origin_secret \
            TF_VAR_rsa_private_key_pem \
            TF_VAR_workers_subdomain
          do
            if [[ -z "${!input_name:-}" ]]; then
              missing_inputs+="${input_name} "
            fi
          done

          if [[ -n "$missing_inputs" ]]; then
            printf '::notice::Terraform plan skipped; missing CI inputs: %s\n' "${missing_inputs% }"
            printf 'ready=false\n' >> "$GITHUB_OUTPUT"
            exit 0
          fi

          printf 'ready=true\n' >> "$GITHUB_OUTPUT"

      - name: Terraform init
        working-directory: terraform
        if: steps.terraform_plan_inputs.outputs.ready == 'true'
        run: terraform init -input=false

      - name: Terraform plan
        working-directory: terraform
        if: steps.terraform_plan_inputs.outputs.ready == 'true'
        run: terraform plan -input=false -no-color
